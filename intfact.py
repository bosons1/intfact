#!/usr/bin/python3
import sys
from prime import *

def factorize(measurement):
    factor = ""
    return factor

num=str(sys.argv[1])
d1, d2 = determine_pinch_off(num)
factor1 = factorize(d1)
factor2 = factorize(d2)
